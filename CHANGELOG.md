# 版本记录

## v0.2.3 — 2026-09-03

支持编辑已有 Alarkive Package：四个 Content Variant 自动回填并可补充，v0.2 原地更新；v0.1 在保存时原地升级为 v0.2，将 `baijiahao` 映射为 `public_long`、`wechat` 映射为 `wechat_short`。任务 ID、名称、创建时间和发布状态保持不变，未选择替换时沿用原图片；旧小红书正文文件保留但不纳入 v0.2 内容模块。编辑通过完整临时目录校验后整体替换，避免正文文件与 `manifest.json` 不一致。

## v0.2.2 — 2026-09-03

接入微头条，完成目前四种发布方式的一键发布：百家号 → 今日头条文章 → 微信图文 → 微头条。各平台自动填写到预发布状态，最终发布由用户手动操作。

### 微头条预发布

- 新增独立 `toutiao_micro.py`，只消费 `post.toutiao_short` 的标题、正文和图片。百家号及微信图文发布代码保持不变。
- 启用 `toutiao_micro` 路由、详情页单平台入口和全流程目标；全流程将微头条追加在微信图文之后。
- 微头条没有独立标题栏，使用标题首段 + 空行 + 正文；经 ProseMirror 粘贴处理写入模型，保留正文换行和空段，不写入 Markdown 强调符号。
- 图片按 Package 顺序逐张上传，校验成功状态、已加载缩略图和追加顺序，再通过图片抽屉内的“确定”附加到独立图片区。不会解析图片占位符或向正文插图。
- 处理发文助手遮罩；已有内容恢复时停止，避免混用旧文字或图片；失败时保存本地诊断并保留浏览器。
- 最终检查文字、空行、图片数量和顺序，确认图片不在正文内，然后等待人工检查。禁止点击最终发布控件；不执行“存草稿”，不把预发布等同于远程草稿保存成功。

### 全流程衔接修复

- 修复头条文章已正确填入、但“草稿保存中”超过 30 秒导致全流程失败的问题：仅将保存等待超时转为明确提示，保留人工检查和继续入口，不宣称草稿已保存。
- 全流程在用户点击继续后进入微信图文、微头条；单平台同样显示保存状态提示，并等待用户结束流程。提示保留在头条的平台状态消息中。
- 标题、正文、图片及发布区域的校验仍然有效，实际内容或页面检查失败仍会中止；未修改文章填入、插图或上传逻辑。

### 验证范围

- 使用真实 Windows Google Chrome 和最新 `posts/20260902-233140-40e3`，经现有单平台 workflow 完成测试：微头条自己的标题、完整正文、段落空行和 4 张独立配图均通过页面数据及截图检查，图片顺序为 `01.png` → `04.png`。
- 158 项测试通过，包含微头条路由、单平台隔离、独立内容读取、图片顺序与失败、空行保留、最终发布按钮防护及 Web 入口显示。新增使用真实 Web 控制器和模拟浏览器 I/O 的回归测试，验证草稿超时后暂停、用户继续、后续两个平台运行，以及单平台结束流程。
- 用户已在真实 Chrome 中确认微头条单平台准备成功，并在草稿保存等待修复后确认四种发布方式的一键流程成功。
- 调试脚本默认按 Package 创建时间选取最新包，完成后暂停并保留 Chrome；测试前后 Package 所有文件 SHA-256 一致。
- 自动化未点击最终发布或存草稿按钮，未改动真实 Package 内容。

## v0.2.1 — 2026-09-03

### 今日头条文章预发布

- 保持 Package v0.2、Content Variant 与 Routing 不变，继续消费 `post.public_long`。
- 在项目 `start_browser()` 启动的真实 Google Chrome 中填写标题、渲染正文，并按 `[[image:N]]` 顺序插入正文图片；未引用图片按原图片列表顺序追加末尾。
- 每次插图前先恢复正文编辑器 caret，再查找编辑器所属 toolbar；上传和确认均限制在因图片按钮打开的 UI 内，不回退到全页面 file input。
- 根据新增缩略图、明确上传成功状态和稳定观察判断上传完成；选择新增图片而非旧素材。
- 修复成功标记没有可见尺寸而误判上传失败的问题。
- 修复 Syl / ProseMirror 对同一图片同时渲染模型节点与预览节点，导致图片数量翻倍的问题。
- 验证编辑器重绘后的正文、逻辑图片数量以及图文交替顺序；防御性拒绝可能提交或发布文章的按钮。
- 增加仅运行头条的真实 Chrome 调试脚本和失败诊断。截图、DOM、网络诊断及浏览器 profile 不随版本提交。

### 验证范围

- 用户已在真实 Windows Chrome 中确认：最新真实 Package 的完整正文与 4 张末尾图片正确；随后确认 4 张图片按标记进入正文，位置全部正确。
- 123 项单元测试通过，包含 marker 顺序、partial marker、未引用图片、no-marker、focus 顺序、scoped 上传、素材选择、确认与发布防护。
- 上述单测场景不代表每种组合都经过独立真实 Chrome 人工验收。
- 未点击任何最终发布按钮。未改动真实 `posts/` 数据、百家号或微信图文发布代码。

### 已知问题

- 实测头条自动草稿请求返回业务错误码 `7050`，底部持续显示“草稿保存中”。具体原因尚未查清，不能据 HTTP 200 判定保存成功。
- v0.2.1 中，若保存状态超过 30 秒仍未结束，最终检查会报错并保留浏览器供人工检查，不会进入“准备完成”状态。v0.2.2 已改为带提示的人工检查与继续步骤；文章已填入页面不等于草稿已保存。
- 头条编辑器 toolbar、drawer、上传状态和 Syl 图片节点依赖平台实际 DOM；平台升级后可能需要重新验证。
